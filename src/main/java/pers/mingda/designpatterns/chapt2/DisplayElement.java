package pers.mingda.designpatterns.chapt2;

// The DisplayElement interface just includes
// one method, display(), that we will call when
// the dispaly element needs to be displayed.
public interface DisplayElement {
    public void display();
}
